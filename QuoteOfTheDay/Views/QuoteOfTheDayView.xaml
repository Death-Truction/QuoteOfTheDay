<UserControl x:Class="QuoteOfTheDay.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:QuoteOfTheDay.Views"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <TextBlock VerticalAlignment="Center" TextAlignment="Center"
                   Text="{Binding QuoteText}" FontSize="{Binding Settings.QuoteFontSize}"
                   FontFamily="{Binding Settings.QuoteFont}" FontStyle="{Binding Settings.QuoteFontStyle}"
                   Background="Transparent" TextWrapping="Wrap" />
    </Grid>
</UserControl>